deploy:
  - okteto build -t okteto.dev/idex:${OKTETO_GIT_COMMIT} --build-arg GITHUB_TOKEN=${GIT_TOKEN} --build-arg strip=true idex
  - helm upgrade --install idex chart --set tag=${OKTETO_GIT_COMMIT}
